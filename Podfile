# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TasKClicks' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  # Pods for TasKClicks
  pod 'NVActivityIndicatorView'
  pod 'Alamofire', '~> 4.2'
  pod 'Kingfisher', '~> 4.0'

end
